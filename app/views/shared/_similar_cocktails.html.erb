<div class="tasty_selection">
  <h1>
    Tasty selection:
  </h1>
  <div style="display: flex; flex-wrap: wrap;" class="align-center container justify-content-around">
    <% @cocktails.each do |cocktail| %>
      <%= render 'shared/cocktail_card', cocktail_within: cocktail %>
    <% end %>
  </div>
</div>
