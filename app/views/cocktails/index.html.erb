<%= render 'shared/banner'%>
<div class="container text-center">
<h1>Cocktails</h1>
</div>
<div class="container ">
  <div class="row">
    <% @cocktails.each do |cocktail| %>
      <%= link_to (render 'shared/card', cocktail_within: cocktail),  "/cocktails/#{cocktail.id}"%>
    <% end %>
  </div>
</div>
